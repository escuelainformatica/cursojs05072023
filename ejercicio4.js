let paises=["Chile","Argentina","Peru","Bolivia"];

paises.map(
    function(valor) { 
        console.log(valor); 
    }
    );
//         argumento/alias => (entonces)    
paises.map(
    pais=> console.log(pais)
    );

