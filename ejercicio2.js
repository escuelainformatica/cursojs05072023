// nomenclatura de nombres.
// ejercicio de variable
// las variables se recomienda que partan en minuscula.
// las variables usualmente se refieren a propiedades o atributos. "color","edad", etc.
// las funciones se recomiendan que partan en minuscula.
// las funciones por lo general se refieren a acciones o verbos. "mostrar","sumar","calcular", etc.
// comunmente los campos/propiedades tambien se inician con minuscula.
// comunmente los argumentos de una funcion se inician con minuscula.

// variables:
let nombreApellido="John Doe"; // OK (camel case o formato del camello)
let nombreapellido="John Doe"; // OK pero no queda claro.
let NombreApellido="John Doe"; // no es correcto
let nombre_apellido="John Doe"; // OK, pero es antigua. (formato de la serpiente)

// funciones:
function sumar() {
}
function obtenerEdad() { // usualmente se escribe getEdad()
}

// campos
let cliente={
    nombre:"John" // nombre es el campo o propiedad
} 
// funcion con argumentos
function sumar(argumento1,argumento2) {

}
